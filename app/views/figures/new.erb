
<h1>Create a new Figure</h1>
<form action="/figures" method="POST">
  <label>Figures</label>
  <br></br>

<p>Figure Name: <input type="text" name="figure[name]" id="figure_name"></p>
  <br></br>

<p>Select Title: <% Title.all.each do |title| %>
    <input id="<%= title.id %>" type="checkbox" name="title_<%= title.id %>" value="title_<%= title.id %>"><%=title.name%></input></p>
  <% end %>
  <label>And/or, create a new Title with a name :</label>
    <input  type="text" name="new_title"></input>
  <br></br>

<p>Select Landmark: <% Landmark.all.each do |landmark| %>
      <input id="<%= landmark.id %>" type="checkbox" name="landmark_<%= landmark.id %>" value="landmark_<%= landmark.id %>"><%=landmark.name%></input></p>
    <% end %>
    <label>And/or, create a new Landmark with a name :</label>
      <input  type="text" name="landmark[name]"></input>

    <input type="submit" value="Create New Figure" name="Create New Figure">
  </form>
