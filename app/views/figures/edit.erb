
<h1>Create a new Figure</h1>
<form action="/figures/<%= @figure.id %>" method="POST">
  <label>Figures</label>
<p>Figure Name: <input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name %>"></p>

<p>Select Title: <% Title.all.each do |title| %>
    <input id="title_<%= title.id %>" type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" ><%=title.name%></input></p>
  <% end %>
  <label>And/or, create a new Title with a name :</label>
    <input  type="text" name="title[name]" id="new_title" ></input>

<p>Select Landmark: <% Landmark.all.each do |landmark| %>
      <input name="figure[landmark_ids][]" type="checkbox" id="landmark_<%= landmark.id %>" value ="<%= landmark.id %>"><%=landmark.name%></input></p>
    <% end %>
    <label>And/or, create a new Landmark with a name :</label>
      <input  type="text" name="landmark[name]" id="new_landmark"></input>
      <input  type="text" name="landmark[year_completed]" id="new_landmark_year_completed"></input>

    <input type="submit" value="Edit Figure" name="Edit Figure">
  </form>
